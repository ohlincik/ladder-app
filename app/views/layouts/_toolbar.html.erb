<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "LECOM Ladder", root_path, id: 'logo', class: 'navbar-brand' %>
  </div>
  
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <% if player_signed_in? %>
      <ul class="nav navbar-nav">
          <li <%= active_page('players','show') %>>
            <%= link_to player_path(current_player) do %>
              <span class="glyphicon glyphicon-user"></span> Your Player Profile
            <% end %>
          </li>
          <li <%= active_page('players','index') %>>
            <%= link_to players_path do %>
              <span class="glyphicon glyphicon-list"></span> Ladder Rankings
            <% end %>
          </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-cog"></span> My Account <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to edit_player_registration_path do %>
                <span class="glyphicon glyphicon-edit"></span> Edit Your Profile
              <% end %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to destroy_player_session_path, method: 'delete', confirm: 'Do you really want to sign out?' do %>
                <span class="glyphicon glyphicon-eject"></span> Sign out
              <% end %>
            </li>
          </ul>
        </li>
      </ul>
    <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Sign Up", new_player_registration_path %></li>
        <li><%= link_to "Sign in", new_player_session_path %></li>
      </ul>
    <% end %>
  </div><!-- /.navbar-collapse -->
</nav>