Hello <%= @resource.first_name %>!
==================================

Someone has requested to reset your password for the LECOM Ladder website.

If you don't know anything about this, simply ignore this email. Nothing will change.

Otherwise, click the link below and it will take you through the process of getting a new password.

<%= edit_password_url(@resource, :reset_password_token => @token) %>

IMPORTANT: Your password won't change until you click the button above and create a new one.