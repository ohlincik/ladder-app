<div class="row">
  <div class="span6 offset3">

    <h2>Sign up</h2>

    <p>Here you can sign up to participate in LECOM Ladder. Only current members of the LECOM Medical Fitness &amp; Wellness Center are eligible. If you are not currently a member, please visit the <a href="http://www.lecomwellness.com/" target="_blank">LECOM Wellness</a> website first to learn about their memberships.</p>

    <hr>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal' }) do |f| %>
      <%= devise_error_messages! %>

      <%= f.input :first_name, :autofocus => true %>

      <%= f.input :last_name %>

      <%= f.input :phone %>

      <%= f.input :email %>

      <%= f.input :password %>

      <%= f.input :password_confirmation, label: 'Confirm Password' %>

      <div class="form-actions">
        <%= f.button :submit, 'Sign up', class: 'btn-primary', disable_with: 'Signing you up...' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>

  </div>
</div>
